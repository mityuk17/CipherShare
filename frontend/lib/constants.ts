// API URLs
export const API_URL = process.env.PUBLIC_API_URL || "http://localhost:8080/api"

// API endpoints
export const ENDPOINTS = {
  SHARE: `${API_URL}/text-records/`,
  RETRIEVE: `${API_URL}/text-records`,
}

